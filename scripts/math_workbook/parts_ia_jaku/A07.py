# -*- coding: utf-8 -*-
"""A07 2次関数の最大・最小（数学I 2次関数）"""
import os
import sys
from fractions import Fraction as F

import sympy as sp
from sympy import symbols, Interval, Rational as R

_HERE = os.path.dirname(os.path.abspath(__file__))
_ROOT = os.path.dirname(_HERE)
if _ROOT not in sys.path:
    sys.path.insert(0, _ROOT)
from svgfig import Canvas, NAVY, RED, FILL, GRAY  # noqa: E402

x, y, a, t = symbols('x y a t', real=True)

# ★ この単元にグラフの図は付けない。
#   最大・最小は「平方完成して自分でグラフを描き、軸と定義域の位置関係を読む」ことが
#   問われている技能そのもの。完成したグラフを問題編に載せると、頂点が定義域の内側か・
#   どちらの端が高いかが計算前に目で読めてしまい、答え（とくに最大最小をとる x）が漏れる。
#   同書 A06 も、通る3点を問う問題には点だけを打ち、放物線そのものは描いていない。

# --- 図: 壁を1辺に使う長方形の花だん（数値は問題文にある x のみ・寸法は書かない）
_c2 = Canvas(xr=(-0.7, 5.7), yr=(-1.1, 3.9), size=(200, 150))
_c2.seg(-0.5, 3, 5.5, 3, stroke=NAVY, w=2.6)                 # 壁
for _i in range(13):
    _wx = -0.4 + 0.45 * _i
    _c2.seg(_wx, 3, _wx - 0.3, 3.45, stroke=GRAY, w=1.0)     # 壁のハッチ
_c2.poly([(1, 0), (4, 0), (4, 3), (1, 3)], fill=FILL, stroke=NAVY, w=1.0)
_c2.seg(1, 0, 1, 3, stroke=RED, w=2.2)                       # 柵（壁に垂直）
_c2.seg(4, 0, 4, 3, stroke=RED, w=2.2)                       # 柵（壁に垂直）
_c2.seg(1, 0, 4, 0, stroke=RED, w=2.2)                       # 柵（壁に平行）
_c2.label(1, 1.5, "x", dx=-13, dy=4, size=13)
_c2.label(4, 1.5, "x", dx=6, dy=4, size=13)
_c2.label(-0.5, 3, "壁", dx=0, dy=-7, size=12)
_c2.label(2.5, 0, "柵", dx=-6, dy=15, size=12)
FIG_FENCE = _c2.svg()


UNIT = {
 "name": "2次関数の最大・最小",
 "tag": "定義域つき・軸や区間が動くときの場合分け",
 "problems": [

  {"group": "定義域が決まっているときの最大・最小", "level": "basic",
   "skill": "QF3", "sub": ["QF1"],
   "stem": "2次関数 $y=x^{2}-6x+4$ の $0\\leqq x\\leqq 5$ における最大値と最小値を、"
           "それぞれそのときの $x$ の値とともに求めよ。",
   "answer": "最大値 $4\\ (x=0)$、最小値 $-5\\ (x=3)$",
   "explanation": r"""▶ 定義域つきの最大・最小は<b>平方完成してグラフを描いてから読む</b>。
式だけ見て「頂点が最小」と決めつけると、軸が定義域の外にあるときに必ず外す。まず軸の位置を出す。
① $y=x^{2}-6x+4=(x-3)^{2}-9+4=(x-3)^{2}-5$。軸は $x=3$、頂点は $(3,\ -5)$、下に凸。
② 軸 $x=3$ は定義域 $0\leqq x\leqq 5$ の<b>内側</b>にある。下に凸だから、最小は頂点そのもので $-5$（$x=3$）。
③ 最大は両端の勝負。$x=0$ のとき $4$、$x=5$ のとき $25-30+4=-1$。
④ 大きいほうは $4$。よって最大値 $4$（$x=0$）。
★ 最大をとる端は<b>軸から遠いほう</b>と決まっている（下に凸のとき）。
ここは軸 $x=3$ から左端まで $3$、右端まで $2$ なので、計算する前に $x=0$ 側だと予想がつく。
検算は「最小 $\leqq$ もう一方の端の値 $\leqq$ 最大」に収まるか見るだけ: $-5\leqq -1\leqq 4$ ✓""",
   # 値だけでなく「そのときの x」も照合する(設問が x を答えの一部として要求しているため)。
   "chk": lambda: (sp.maximum(x**2 - 6*x + 4, x, Interval(0, 5)) == 4
                   and sp.minimum(x**2 - 6*x + 4, x, Interval(0, 5)) == -5
                   and sp.solveset(sp.Eq(x**2 - 6*x + 4, 4), x, Interval(0, 5))
                       == sp.FiniteSet(0)
                   and sp.solveset(sp.Eq(x**2 - 6*x + 4, -5), x, Interval(0, 5))
                       == sp.FiniteSet(3))},

  {"group": "定義域が決まっているときの最大・最小", "level": "basic",
   "skill": "QF3", "sub": ["QF1"],
   "stem": "2次関数 $y=-2x^{2}+8x-5$ の $3\\leqq x\\leqq 5$ における最大値と最小値を、"
           "それぞれそのときの $x$ の値とともに求めよ。",
   "answer": "最大値 $1\\ (x=3)$、最小値 $-15\\ (x=5)$",
   "explanation": r"""▶ $x^{2}$ の係数が負なので<b>上に凸</b>。上に凸なら頂点は最大の候補だが、
その頂点が定義域に入っていなければ候補にならない。ここでも先に軸を出して定義域と比べる。
① $y=-2x^{2}+8x-5=-2(x^{2}-4x)-5=-2\{(x-2)^{2}-4\}-5=-2(x-2)^{2}+3$。
<b>$-2$ でくくったので、かっこを外すとき $-2\times(-4)=+8$</b> になる。軸は $x=2$、頂点は $(2,\ 3)$。
② 軸 $x=2$ は定義域 $3\leqq x\leqq 5$ より<b>左の外</b>。上に凸のグラフは軸より右で右下がりなので、
定義域の中ではずっと減り続ける。
③ よって最大は左端 $x=3$: $-2\cdot 1^{2}+3=1$。最小は右端 $x=5$: $-2\cdot 3^{2}+3=-15$。
★ 事故の1位は<b>「上に凸だから最大は頂点の $3$」と機械的に書くこと</b>。
頂点 $(2,\ 3)$ は定義域の外にあり、$x=2$ は使えない。
<b>軸が定義域の外なら、その区間でグラフは単調</b>＝最大も最小も両端で決まる、と覚える。""",
   # ★ この問題の核心は「どちらの端が最大か」なので、値だけでなく x も必ず照合する。
   #   値だけだと最大と最小の x を入れ替えた誤答が素通りする。
   "chk": lambda: (sp.maximum(-2*x**2 + 8*x - 5, x, Interval(3, 5)) == 1
                   and sp.minimum(-2*x**2 + 8*x - 5, x, Interval(3, 5)) == -15
                   and sp.solveset(sp.Eq(-2*x**2 + 8*x - 5, 1), x, Interval(3, 5))
                       == sp.FiniteSet(3)
                   and sp.solveset(sp.Eq(-2*x**2 + 8*x - 5, -15), x, Interval(3, 5))
                       == sp.FiniteSet(5))},

  {"group": "定義域が決まっているときの最大・最小", "level": "standard",
   "skill": "QF3", "sub": ["QF1"],
   "stem": "2次関数 $y=2x^{2}-5x+1$ の $0\\leqq x\\leqq 2$ における最大値と最小値を、"
           "それぞれそのときの $x$ の値とともに求めよ。",
   "answer": "最大値 $1\\ (x=0)$、最小値 $-\\dfrac{17}{8}\\ \\left(x=\\dfrac{5}{4}\\right)$",
   "explanation": r"""▶ 軸が分数になる型。$x^{2}$ の係数 $2$ を<b>先にくくり出してから</b>平方完成しないと、
軸が半分ずれる。手順を崩さないことがすべて。
① $y=2x^{2}-5x+1=2\left(x^{2}-\frac{5}{2}x\right)+1$。
② かっこの中だけ平方完成する。引くのは「$x$ の係数の半分の2乗」なので
$x^{2}-\frac{5}{2}x=\left(x-\frac{5}{4}\right)^{2}-\frac{25}{16}$。
③ かっこの外へ戻すとき<b>$-\frac{25}{16}$ にも $2$ を掛ける</b>:
$y=2\left(x-\frac{5}{4}\right)^{2}-\frac{25}{8}+1=2\left(x-\frac{5}{4}\right)^{2}-\frac{17}{8}$。
④ 軸 $x=\frac{5}{4}$ は $0\leqq x\leqq 2$ の内側なので、最小値は $-\frac{17}{8}$（$x=\frac{5}{4}$）。
⑤ 端点は $x=0$ で $1$、$x=2$ で $8-10+1=-1$。大きいのは $1$ なので最大値 $1$（$x=0$）。
★ ③で<b>くくり出した $2$ を掛け忘れて $-\frac{25}{16}$ と書く</b>のが最頻出の事故。
防ぎ方は1つ、軸の値を<b>元の式に直接代入して頂点の $y$ を出し直す</b>こと:
$2\cdot\frac{25}{16}-5\cdot\frac{5}{4}+1=\frac{25}{8}-\frac{50}{8}+\frac{8}{8}=-\frac{17}{8}$ ✓ 一致したので安心してよい。""",
   "chk": lambda: (sp.maximum(2*x**2 - 5*x + 1, x, Interval(0, 2)) == 1
                   and sp.minimum(2*x**2 - 5*x + 1, x, Interval(0, 2)) == R(-17, 8)
                   and sp.solveset(sp.Eq(2*x**2 - 5*x + 1, 1), x, Interval(0, 2))
                       == sp.FiniteSet(0)
                   and sp.solveset(sp.Eq(2*x**2 - 5*x + 1, R(-17, 8)), x, Interval(0, 2))
                       == sp.FiniteSet(R(5, 4)))},

  {"group": "軸や定義域に文字を含むとき", "level": "standard",
   "skill": "QF4", "sub": ["QF1"],
   "stem": "$a$ を定数とする。2次関数 $y=x^{2}-4ax+1$ の $0\\leqq x\\leqq 2$ における"
           "最小値 $m$ と最大値 $M$ を、それぞれ $a$ の値で場合分けして求めよ。",
   "answer": "最小値 $m$ は $a\\lt 0$ のとき $1\\ (x=0)$、$0\\leqq a\\leqq 1$ のとき "
             "$1-4a^{2}\\ (x=2a)$、$a\\gt 1$ のとき $5-8a\\ (x=2)$。"
             "最大値 $M$ は $a\\leqq\\dfrac{1}{2}$ のとき $5-8a\\ (x=2)$、"
             "$a\\geqq\\dfrac{1}{2}$ のとき $1\\ (x=0)$",
   "explanation": r"""▶ 定義域 $0\leqq x\leqq 2$ は固定で、<b>軸のほうが $a$ とともに左右に動く</b>。
動くものと止まっているものを取り違えないこと。そのうえで<b>最小と最大は分ける境目が違う</b>ので、
片方の場合分けをそのまま流用しない。図を描いて1つずつ確かめる。
① 平方完成: $y=x^{2}-4ax+1=(x-2a)^{2}-4a^{2}+1$。軸は $x=2a$、下に凸。
② 最小は<b>「軸が定義域より左」「軸が定義域の中」「軸が定義域より右」</b>の3通り。
軸が左（$2a\lt 0$ すなわち $a\lt 0$）: 定義域の中では右上がり一方なので最小は左端 $x=0$ で $m=1$。
③ 軸が定義域の中（$0\leqq 2a\leqq 2$ すなわち $0\leqq a\leqq 1$）: 最小は頂点そのもので
$x=2a$ のとき $m=1-4a^{2}$。
④ 軸が定義域より右（$2a\gt 2$ すなわち $a\gt 1$）: 右下がり一方なので最小は右端 $x=2$。
$m=4-8a+1=5-8a$。
⑤ 最大は分け方が変わる。下に凸なら最大は<b>軸から遠いほうの端</b>で、遠近が入れかわるのは
軸が定義域の<b>中点 $x=1$</b> に来たとき。$2a\lt 1$ すなわち $a\lt\frac{1}{2}$ なら遠い端は $x=2$ で
$M=4-8a+1=5-8a$、$a\gt\frac{1}{2}$ なら遠い端は $x=0$ で $M=1$。
$a=\frac{1}{2}$ では両端が軸から等距離なので $x=0$ でも $x=2$ でも $M=1$ になる
（$5-8\times\frac{1}{2}=1$ と一致する）。よって最大は<b>2通り</b>で足りる。
★ 分ける<b>境目は、最小なら「軸＝端点」、最大なら「軸＝定義域の中点」</b>。
最小の $a=0,\ 1$ という境目を最大にも使い回して3通りに分けるのが事故の1位で、
最大の境目は $a=\frac{1}{2}$ ただ1つである。
検算は境目でやる。<b>境目では隣り合う場合の答えが必ず一致する</b>:
$a=0$ なら③は $1-0=1$ で②の $1$ と一致、$a=1$ なら③は $1-4=-3$、④は $5-8=-3$ で一致 ✓
$a=\frac{1}{2}$ なら⑤の $5-8a=1$ でもう一方の $1$ と一致 ✓
ここが合わなければ、どこかで代入か符号を間違えている。""",
   # 値だけでなく最小・最大をとる x も照合する(場合分けの境目そのものが問われているため)。
   "chk": lambda: all(
       (lambda f=x**2 - 4*A*x + 1,
               m=(1 if A < 0 else (1 - 4*A**2 if A <= 1 else 5 - 8*A)),
               xm=(0 if A < 0 else (2*A if A <= 1 else 2)),
               Mv=(5 - 8*A if A <= R(1, 2) else 1),
               xM=(sp.FiniteSet(0, 2) if A == R(1, 2)
                   else sp.FiniteSet(2 if A < R(1, 2) else 0)):
        sp.minimum(f, x, Interval(0, 2)) == m
        and sp.solveset(sp.Eq(f, m), x, Interval(0, 2)) == sp.FiniteSet(xm)
        and sp.maximum(f, x, Interval(0, 2)) == Mv
        and sp.solveset(sp.Eq(f, Mv), x, Interval(0, 2)) == xM)()
       for A in (R(-3, 2), R(-1, 4), 0, R(1, 4), R(1, 2), R(3, 4), 1, R(5, 4), 3))},

  {"group": "軸や定義域に文字を含むとき", "level": "advanced",
   "skill": "QF4", "sub": ["QF1"],
   "stem": "$a$ を定数とする。2次関数 $f(x)=x^{2}-2x+4$ の $a\\leqq x\\leqq a+2$ における最小値 $m$ を、"
           "$a$ の値で場合分けして求めよ。",
   "answer": "$a\\lt -1$ のとき $m=a^{2}+2a+4\\ (x=a+2)$、$-1\\leqq a\\leqq 1$ のとき $m=3\\ (x=1)$、"
             "$a\\gt 1$ のとき $m=a^{2}-2a+4\\ (x=a)$",
   "explanation": r"""▶ 前問と逆で、<b>軸が止まっていて定義域のほうが動く</b>。
幅 $2$ の窓が左から右へすべっていくと思えばよい。分けるのはやはり
<b>「窓が軸より左」「軸が窓の中」「窓が軸より右」</b>の3通り。
① 平方完成: $f(x)=x^{2}-2x+4=(x-1)^{2}+3$。軸は $x=1$、下に凸で固定。
② 窓が軸より左（$a+2\lt 1$ すなわち $a\lt -1$）: 窓の中では右下がりなので最小は<b>右端</b> $x=a+2$。
$m=f(a+2)=(a+2-1)^{2}+3=(a+1)^{2}+3=a^{2}+2a+4$。
③ 軸が窓の中（$a\leqq 1$ かつ $1\leqq a+2$ すなわち $-1\leqq a\leqq 1$）: 最小は頂点で $m=3$（$x=1$）。
④ 窓が軸より右（$a\gt 1$）: 窓の中では右上がりなので最小は<b>左端</b> $x=a$。
$m=f(a)=(a-1)^{2}+3=a^{2}-2a+4$。
★ ③の条件を $a\leqq 1\leqq a+2$ から<b>$a$ だけの不等式に直す</b>のを面倒がらないこと。
$1\leqq a+2$ は $a\geqq -1$ なので、あわせて $-1\leqq a\leqq 1$ となる。
また②④の代入は<b>平方完成した形 $(x-1)^{2}+3$ に入れる</b>ほうが速く、符号ミスも減る。
境目で検算: $a=-1$ のとき②は $1-2+4=3$ で③と一致、$a=1$ のとき④は $1-2+4=3$ で③と一致 ✓""",
   # 最小値をとる x（左端 $a$ か右端 $a+2$ か頂点 $1$ か）も照合する。
   "chk": lambda: all(
       (lambda m=(A**2 + 2*A + 4 if A < -1 else (3 if A <= 1 else A**2 - 2*A + 4)),
               xm=(A + 2 if A < -1 else (1 if A <= 1 else A)):
        sp.minimum(x**2 - 2*x + 4, x, Interval(A, A + 2)) == m
        and sp.solveset(sp.Eq(x**2 - 2*x + 4, m), x, Interval(A, A + 2))
            == sp.FiniteSet(xm))()
       for A in (-4, R(-3, 2), -1, 0, R(1, 2), 1, R(3, 2), 4))},

  {"group": "置きかえて2次関数にする", "level": "standard",
   "skill": "QF3", "sub": ["QF1"],
   "stem": "$-1\\leqq x\\leqq 3$ のとき、$y=(x^{2}-2x)^{2}+4(x^{2}-2x)+1$ の最大値と最小値を、"
           "それぞれそのときの $x$ の値とともに求めよ。",
   "answer": "最大値 $22\\ (x=-1,\\ 3)$、最小値 $-2\\ (x=1)$",
   "explanation": r"""▶ 同じかたまり $x^{2}-2x$ が2回出ている → $t=x^{2}-2x$ とおけば $t$ の2次関数になる。
ただし<b>置きかえたら、次にやることは $t$ の動ける範囲を出すこと</b>。ここを飛ばすのが最大の事故。
① $t=x^{2}-2x=(x-1)^{2}-1$。$-1\leqq x\leqq 3$ での $t$ の値域を調べる。
軸 $x=1$ はこの定義域の内側なので $t$ の最小は $-1$。端点は $x=-1$ で $1+2=3$、$x=3$ で $9-6=3$ だから
$t$ の最大は $3$。よって<b>$-1\leqq t\leqq 3$</b>。
② 元の式は $y=t^{2}+4t+1=(t+2)^{2}-3$。軸は $t=-2$。
③ 軸 $t=-2$ は $-1\leqq t\leqq 3$ の<b>外（左）</b>にある。この範囲で $y$ は増える一方なので、
最小は $t=-1$ のとき $1-4+1=-2$、最大は $t=3$ のとき $9+12+1=22$。
④ 答えは $x$ で聞かれているので<b>戻す</b>。$t=-1$ は $x^{2}-2x+1=0$ より $x=1$。
$t=3$ は $x^{2}-2x-3=0$、すなわち $(x+1)(x-3)=0$ より $x=-1,\ 3$。
★ 事故の1位は<b>「頂点だから $t=-2$ で最小値 $-3$」と答えること</b>。
$t$ は $-1$ 以上しか取れないので $t=-2$ になる実数 $x$ は存在せず、$-3$ は現れない。
置きかえの問題は<b>「$t$ の範囲を書く」「最後に $x$ へ戻す」</b>の2つで点が決まる。
検算は適当な $x$ を入れて範囲に収まるか見る: $x=0$ なら $t=0$ で $y=1$。たしかに $-2\leqq 1\leqq 22$ ✓""",
   "chk": lambda: (
       sp.maximum((x**2 - 2*x)**2 + 4*(x**2 - 2*x) + 1, x, Interval(-1, 3)) == 22
       and sp.minimum((x**2 - 2*x)**2 + 4*(x**2 - 2*x) + 1, x, Interval(-1, 3)) == -2
       and sp.solveset(sp.Eq((x**2 - 2*x)**2 + 4*(x**2 - 2*x) + 1, 22), x, sp.S.Reals)
           == sp.FiniteSet(-1, 3)
       and sp.solveset(sp.Eq((x**2 - 2*x)**2 + 4*(x**2 - 2*x) + 1, -2), x, sp.S.Reals)
           == sp.FiniteSet(1))},

  {"group": "文章題（面積を最大にする）", "level": "standard",
   "skill": "QF3", "sub": ["QF1", "SK7"],
   "stem": "長さ $20$ m の柵（さく）を使い、まっすぐな壁を1辺として利用して、"
           "図のような長方形の花だんを作る。柵は壁に垂直な2辺と壁に平行な1辺に使い、"
           "余りは出ないものとする。壁に垂直な辺の長さを $x$ m とする。<br>"
           "(1) 花だんの面積 $S$ を $x$ の式で表せ。<br>"
           "(2) 壁に平行な辺の長さを $8$ m 以下にするとき、$S$ の最大値を求めよ。",
   "answer": "(1) $S=-2x^{2}+20x$　(2) 最大値 $48\\ \\mathrm{m}^{2}\\ (x=6)$",
   "figure": FIG_FENCE,
   "explanation": r"""▶ 文章題は<b>「文字でおく → 式を作る → 定義域を書く」の3段</b>。
3段目を書かないと、頂点の値をそのまま答えて外す。この問題はまさにその型になっている。
① 壁に垂直な辺が $x$ m で2本、残りが壁に平行な1辺だから、その長さは $20-2x$ (m)。
② (1) 面積は縦×横で $S=x(20-2x)=-2x^{2}+20x$。
③ (2) 定義域を作る。長さは正なので $x\gt 0$ と $20-2x\gt 0$、すなわち $x\lt 10$。
さらに条件「壁に平行な辺は $8$ m 以下」から $20-2x\leqq 8$、これを解いて $x\geqq 6$。
あわせて<b>$6\leqq x\lt 10$</b>。
④ $S=-2(x^{2}-10x)=-2(x-5)^{2}+50$ なので軸は $x=5$、上に凸。
<b>軸 $x=5$ は定義域 $6\leqq x\lt 10$ の外（左）</b>だから、この範囲で $S$ は減る一方。
⑤ 最大は左端 $x=6$ のとき。$S=6\times(20-12)=6\times 8=48$。よって最大値 $48\ \mathrm{m}^{2}$。
★ ここで $50$ と答えたら、③の定義域を書かなかった証拠。
<b>頂点が定義域に入っているかを必ず確かめる</b>のがこの単元の合言葉。
検算は範囲内の別の値で: $x=7$ なら $S=7\times 6=42$、$x=8$ なら $S=8\times 4=32$。
$x$ が増えるほど減っており、たしかに左端の $48$ が最大だと分かる ✓""",
   # (1) は「答えの S から壁に平行な辺 S/x を復元し、柵の総延長 2x+S/x が与件の 20 m に
   #     戻るか」で検算する（解説の $S=x(20-2x)$ を打ち直すと恒真式になり検査にならない）。
   # (2) は 0.1 刻みの全走査で最大を独立に見つける。
   "chk": lambda: (
       sp.simplify((-2*x**2 + 20*x)/x + 2*x) == 20
       and max((F(i, 10)*(20 - 2*F(i, 10)), F(i, 10))
               for i in range(1, 100) if 20 - 2*F(i, 10) <= 8) == (48, 6))},
 ]}
