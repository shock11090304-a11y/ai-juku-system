# -*- coding: utf-8 -*-
"""複素数平面・確率分布・極限の巻の「この単元のキモ」オーバーレイ（POINTS のみ）。
問題ごとの詳細解説は content_kisoIII.py の explanation を使う（EXPL は空）。"""

EXPL = {}

POINTS = {
    1: r"""▶ 複素数平面は「複素数＝平面上の点」と見るだけ。$a+bi$ は点 $(a,\,b)$。
① $|z|=\sqrt{a^2+b^2}$ ＝ 原点からの距離。$\bar z$ ＝ 実軸に関して対称な点。
② $z\bar z=|z|^2$（必ず実数）。分母の実数化はこれを使う。
③ 極形式 $z=r(\cos\theta+i\sin\theta)$。$r=|z|$、$\theta$ は偏角。
★かけ算は【絶対値はかけ算、偏角は足し算】、わり算は【絶対値はわり算、偏角は引き算】。
　これが極形式を使う最大の理由。足し算・引き算は $a+bi$ の形のままのほうが速い。
★ド・モアブル $(\cos\theta+i\sin\theta)^n=\cos n\theta+i\sin n\theta$。
　累乗は極形式にしてから。$(1+i)^{10}$ のような問題は必ずこれ。
★$i^2=-1$ の置きかえ忘れが最頻出のミス。展開したら必ず確認する。""",

    2: r"""▶ 複素数平面の図形問題は「引く・回す・足す」の3手順に落とす。
① 2点 $\alpha,\ \beta$ の距離は $|\beta-\alpha|$。
② 原点まわりの $\theta$ 回転は $(\cos\theta+i\sin\theta)$ をかける。
　$\dfrac{\pi}{2}$ 回転は $i$ をかけるだけ。
③ 中心が $\alpha$ の回転は $w=\alpha+(\cos\theta+i\sin\theta)(z-\alpha)$。
　【中心を原点に引き寄せ → 回して → もどす】。この形を覚える。
★軌跡は $|z-\alpha|=r$ が中心 $\alpha$・半径 $r$ の円、
　$|z-\alpha|=|z-\beta|$ が2点の垂直二等分線。距離の言い換えとして読む。
★$\dfrac{\beta-\alpha}{\gamma-\alpha}$ の偏角は $\angle\beta\alpha\gamma$。
　この値が実数なら3点は一直線上、純虚数なら垂直。""",

    3: r"""▶ 確率分布は「期待値と分散の性質」を正確に。ここだけで大半が解ける。
① $E(X)=\sum x_i p_i$（値×確率の合計）。
② $V(X)=E(X^2)-\{E(X)\}^2$。標準偏差 $\sigma(X)=\sqrt{V(X)}$。
③ $E(aX+b)=aE(X)+b$ ／ $V(aX+b)=a^2V(X)$。
★★ここが最重要。期待値は $a$ 倍・$b$ 足しがそのまま効くが、
　分散は【$a$ が2乗でかかり、$b$ は消える】。定数を足しても散らばりは変わらないから。
★二項分布 $B(n,\,p)$ は $E(X)=np$、$V(X)=np(1-p)$。反復試行を見たらこれ。
★標準化 $Z=\dfrac{X-m}{\sigma}$ をすると、必ず $E(Z)=0$、$\sigma(Z)=1$ になる。
　正規分布の問題は標準化して数表を引く、が定型。""",

    4: r"""▶ 統計的な推測は「標本から母集団を言い当てる」。$\sqrt n$ が主役。
① 標本平均 $\bar X$ について $E(\bar X)=m$（母平均と同じ）、
　標準偏差は $\dfrac{\sigma}{\sqrt n}$。標本を大きくすると $\sqrt n$ に反比例して小さくなる。
② 信頼度 $95\%$ の信頼区間は $\bar X \pm 1.96\dfrac{\sigma}{\sqrt n}$。
　信頼度を $99\%$ に上げると係数が $2.58$ になり、区間は【広く】なる。
③ 仮説検定は「主張したいことの否定」を帰無仮説 $H_0$ に立てる。
　$H_0$ のもとで観測結果が起こる確率が有意水準より小さければ $H_0$ を棄却する。
★両側検定の有意水準 $5\%$ は左右に $2.5\%$ ずつなので境界は $1.96$。
　片側検定なら片方に $5\%$ を寄せるので $1.64$。両側か片側かを必ず読み取る。
★「$H_0$ を棄却できなかった」は「$H_0$ が正しいと証明できた」ではない。ここを混同しない。""",

    5: r"""▶ 数列の極限は「どの項がいちばん強いか」を見る。
① $\dfrac1n \to 0$、$\dfrac{1}{n^2}\to0$。これが出発点。
② $\dfrac{\infty}{\infty}$ の形は【分母の最高次で分子・分母を割る】。
　$\dfrac{3n^2-n}{2n^2+5}$ なら $n^2$ で割って $\dfrac{3-\frac1n}{2+\frac5{n^2}}\to\dfrac32$。
③ 無限等比数列 $r^n$ は $|r|<1$ で $0$、$r=1$ で $1$、それ以外は発散。
★無限等比級数の和は $\dfrac{a}{1-r}$。ただし【$|r|<1$ のときだけ】。
　収束条件の確認を書かないと減点される。
★$\infty-\infty$ の形は、そのままでは判定できない。有理化するか通分して形を変える。""",

    6: r"""▶ 関数の極限は「代入してみる」→ 不定形なら「形を変える」。
① 分母が $0$ にならなければ、そのまま代入すればよい（多項式・分数式は連続）。
② $\dfrac00$ の不定形が出たら【約分できる形にする】。
　分子・分母を因数分解する、または有理化する。
③ $\displaystyle\lim_{x\to0}\dfrac{\sin x}{x}=1$。三角関数の極限の基本公式。
　★これは $x$ が【弧度法】のときだけ成り立つ。度数法では成り立たない。
　微分・積分で必ず弧度法を使う理由がこれ。
★$\displaystyle\lim_{h\to0}\dfrac{f(a+h)-f(a)}{h}$ の形を見たら、微分係数 $f'(a)$ の定義を疑う。
　極限を計算せず、微分してから代入するほうが速い。""",

    7: r"""▶ 数IIIの微分は「公式3つ＋基本の導関数」だけ。組み合わせるとどんな式も微分できる。
① 積 $(fg)'=f'g+fg'$
② 商 $\left(\dfrac fg\right)'=\dfrac{f'g-fg'}{g^2}$（分子は【引き算】。順序を逆にすると符号が変わる）
③ 合成（連鎖律）$\{f(g(x))\}'=f'(g(x))\cdot g'(x)$（外側を微分 × 内側を微分）
★基本の導関数：$(\sin x)'=\cos x$、$(\cos x)'=-\sin x$、$(\tan x)'=\dfrac{1}{\cos^2x}$、
　$(e^x)'=e^x$、$(\log x)'=\dfrac1x$（$x>0$）、$(x^n)'=nx^{n-1}$。
★上の6つの基本公式の中では $\cos$ だけ【マイナスが付く】。符号落としが最多のミス。
　（$\left(\dfrac1x\right)'=-\dfrac{1}{x^2}$ のように、他にも負になる導関数はある。）
★$(\log u)'=\dfrac{u'}{u}$ の形（対数微分）は頻出。分子が分母の微分になっていたら即これ。
★数IIIの $\log$ は底が $e$ の自然対数。底を省略して書く。""",

    8: r"""▶ 微分の応用は、結局「増減表を書く」に集約される。
① 接線は $y-f(a)=f'(a)(x-a)$。まず $f'$ を求め、$x=a$ を代入して傾きを出す。
② 増減は $f'$ の符号。$f'>0$ で増加、$f'<0$ で減少。
③ 極値は $f'=0$ かつ【前後で符号が変わる】点。$f''$ の符号で判定してもよい
　（$f''<0$ なら極大、$f''>0$ なら極小）。
★$f'(a)=0$ だけでは極値と言えない（$y=x^3$ が反例）。符号が変わることが条件。
★凹凸は $f''$ の符号。$f''>0$ で下に凸、$f''<0$ で上に凸。
　変曲点は $f''=0$ かつ【前後で $f''$ の符号が変わる】点。ここも「$=0$ だけでは不十分」。
★$e^{-x}>0$ のように【つねに正の因数】は符号判定から外してよい。残った部分だけ見る。""",

    9: r"""▶ 数IIIの積分は「置換」と「部分積分」の2本柱。どちらを使うか見分ける。
① 置換積分：かっこの中身を $u$ と置いて $du$ に直す。
　$\displaystyle\int f(ax+b)dx$ は $u=ax+b$、$dx=\dfrac{du}{a}$。
② $\dfrac{u'}{u}$ の形（分子が分母の微分）は $\log|u|+C$。見つけたら一撃。
③ 部分積分 $\displaystyle\int fg'=fg-\int f'g$。
　【多項式を微分する側に置く】と次数が下がって終わる。
★$\displaystyle\int\log x\,dx$ は $\log x=1\times\log x$ とみて部分積分。$x\log x-x+C$。
★不定積分は積分定数 $C$ を必ず書く。定積分では消えるので不要。
★$\sin^2x$ や $\cos^2x$ は【半角の公式で次数を下げてから】積分する。""",

    10: r"""▶ 面積・体積は「積分する式を立てるところ」が本番。計算は最後。
① 面積は（上の関数）−（下の関数）を積分する。まず交点を求めて区間を決める。
　$x$ 軸より下にある部分は符号を反転させる（面積は必ず正）。
② $x$ 軸まわりの回転体の体積は $V=\pi\displaystyle\int_a^b y^2\,dx$。
　★積分するのは $y$ ではなく【$y^2$】。2乗を忘れるミスが最多。
③ $y$ 軸まわりなら $V=\pi\displaystyle\int_c^d x^2\,dy$（$x$ を $y$ で表してから）。
★グラフを必ずかく。どちらが上か、どこからどこまでか、を図で決めてから式を立てる。
★$\sin^2x$ の積分が出たら半角。$\displaystyle\int_0^{\pi}\sin^2x\,dx=\dfrac{\pi}{2}$ は頻出。"""
}
