# 過去問 SVOCM 構造解析プリント

生徒が「自分でふった文型が合っているか確かめたい」と持ってきた過去問に対して、
**全文の SVOCM 分解 ＋ 全設問の解答解説** を 1 冊の PDF にして返すための道具立て。

現在の対象（`check.py` の既定＝刷るもの全部）:

| key | 出典 | 中身 |
|---|---|---|
| `keio2018` | 慶應義塾大学 経済学部 2018 大問 I | 本文 8 段落・解析 36 文（設問解説は依頼範囲外） |
| `todai2018` | 東京大学 2018 第 5 問 | 本文 26 段落・解析 69 文 ＋ 設問 (A)〜(G) 解答解説 |

```
python3 scripts/eng_svoc_kaisetsu/check.py                 # ゲート（ソース側）
python3 scripts/eng_svoc_kaisetsu/build.py                 # HTML ＋ PDF
python3 scripts/eng_svoc_kaisetsu/build.py --no-pdf        # HTML だけ（CI 用）
python3 scripts/eng_svoc_kaisetsu/check_pdf.py             # 刷り上がりからの逆照合
STUDENT_NAME="姓 名" python3 scripts/eng_svoc_kaisetsu/build.py   # 宛名入りで刷る
```

## 設計

### 記号法は自前で作らない
分解 DSL（`{S:...}` / `( 副詞 )` / `[ 名詞 ]` / `< 形容詞 >`）は
**`scripts/eng_hinshi_bunkai/core.py` のパーサをそのまま読み込んで**使う。
塾内で記号法が 2 つに割れると生徒が混乱するので、コピーせず import する
（`core.py::_load_hb_core`）。

### 本文は「印字そのまま」だけを持つ（単一ソース）
- `RAW` … 配布された過去問プリントの印字そのまま。空所は `[ 1 ]` `(B29)` `(C)` のまま。
- `FILLS` … 空所に入れる語。
- 解析に使う本文は **毎回 `RAW + FILLS` から作り直す**。埋めた本文は保存しない。

### 相互チェック 3 層（CLAUDE.md 2026-08-16）
| 層 | 実体 | 何を止めるか |
|---|---|---|
| ① 出力物どうしの照合 | `check_pdf.py` | 刷り上がりの PDF を読み直し、本文・全解析文・全和訳・全解答・全引用が紙にあるか。ビルドで落ちた・改ページで消えたを拾う |
| ② 機械検査 | `check.py` | 解析 DSL から復元した英文と本文の全文照合／4 セクションの有無／引用の実在／解答と本文の相互照合／(F) の語数とコンマ |
| ③ 人手の再点検 | — | 正解の一意性（誤答が別解釈で正解にならないか）は機械では見えない。全問を敵対的に読み直す |

`check.py` は最後に **自己検査** を回す。データに変異を 10 種入れて
「全部落ちること」を確かめる。ゲートが無力化されていたらここで落ちる。

### 実際に捕まえた事故（2026-08-27）
- 解析 DSL の `{ }` の中に `< >` や `( )` を書いてしまい、**括弧が英文に漏れた** → ②で検出。
- 下線部の目印を `"will"` という短い文字列で指定していたため、**別の will に下線が付いた** → ①で検出。
  以後 `UNDERLINE` は `(ラベル, 本文で一意なアンカー, 下線を引く部分)` の 3 つ組にし、
  アンカーが本文に 1 回しか出てこないことを②が数える。
- 空所の印を消した跡の**二重スペース**でアンカーが当たらず、**下線が紙から消えた** → ①で検出。
  ビルドは置けなかった下線があれば例外で止まる。

## 解説フォーマット
`server/main.py` の英語生成プロンプトが正典。
**🎯 コアイメージ → 🔬 文構造分析 → 📍 本文の根拠 → ❌ 誤答 NG 理由** の 4 セクション必須。
記述問題には誤答の選択肢が無いので、4 つ目は「よくある誤り」として同じ位置に置く。
`check.py` が 4 つ揃っているかを見る。

## 生徒名は書かない
このリポジトリは PUBLIC。宛名は `STUDENT_NAME` 環境変数からしか入らない
（空なら宛名なしの汎用版）。`scripts/check_no_pii.py` が機械で止める。

## 次に足すとき
1. `content_<key>.py` を作る（`META` / `RAW` / `FILLS` / `PARAS` / 必要なら `QUESTIONS`・`OPTIONS`・`ANSWER_MAP`・`UNDERLINE`）。
2. `check.py` の `ALL` と `load()` に key を足す。★足し忘れると引数なしで検査されない。
3. `build.py` の `main()` に `part_html()` を足す。
4. `check.py` → `build.py` → `check_pdf.py` の順に回す。
